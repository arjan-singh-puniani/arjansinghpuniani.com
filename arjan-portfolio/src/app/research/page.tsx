import type { Metadata } from "next";import { CitationButton } from "@/components/CitationButton";import { publications,researchProjects } from "@/content/publications";
export const metadata:Metadata={title:"Research",description:"Publications, research projects, and technical writing by Arjan Singh Puniani.",alternates:{canonical:"/research"}};
export default function Research(){return <><header className="page-hero"><div className="shell"><p className="eyebrow">Research record</p><h1>Research</h1><p>Published work, active research themes, and technical writing. Bibliographic details appear only when the source record supports them.</p></div></header><section className="section"><div className="shell"><div className="section-head"><div><p className="eyebrow">Publications</p><h2>Peer-reviewed work</h2></div></div>{publications.map(p=>{const citation=`${p.authors} (${p.year}). ${p.title}. ${p.venue}.`;return <article className="publication" key={p.title}><p className="publication-meta">{p.type} · {p.year}</p><h3>{p.title}</h3><p><strong>{p.authors}</strong></p><p>{p.summary}</p><p><strong>Role:</strong> {p.role}</p><CitationButton citation={citation}/></article>})}</div></section><section className="section"><div className="shell bio-grid"><div><p className="eyebrow">Research projects</p><h2>Questions under active development</h2></div><div>{researchProjects.map((p,i)=><article className="publication" key={p}><span className="publication-meta">0{i+1}</span><h3>{p}</h3></article>)}</div></div></section><section className="section"><div className="shell"><div className="notice">A final publication PDF, DOI, or publisher record is still required before the site publishes exact author order, venue, volume, pages, or contribution language.</div></div></section></>}
