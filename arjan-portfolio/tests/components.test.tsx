import React from "react";import { render,screen,fireEvent } from "@testing-library/react";import { describe,expect,it,vi } from "vitest";import { ProjectFilter } from "@/components/ProjectFilter";import { Header } from "@/components/Header";import { VectorTennisLab } from "@/components/VectorTennisLab";
describe("essential UI",()=>{it("renders primary navigation",()=>{render(<Header/>);expect(screen.getByRole("link",{name:"Work"})).toHaveAttribute("href","/work");expect(screen.getByRole("link",{name:"Contact"})).toBeInTheDocument()});it("filters projects",()=>{render(<ProjectFilter/>);fireEvent.click(screen.getByRole("button",{name:"Motorsport Medicine"}));expect(screen.getByText("Mechanism-to-Medical Center")).toBeInTheDocument();expect(screen.queryByText("SeizeFreeze")).not.toBeInTheDocument()})});
describe("Racket Lab controls",()=>{it("retains advanced manual movement and shot selection",()=>{const canvas=vi.spyOn(HTMLCanvasElement.prototype,"getContext").mockReturnValue(null);render(<VectorTennisLab/>);expect(screen.getByRole("button",{name:"Move left"})).toBeInTheDocument();expect(screen.getByRole("button",{name:"Move right"})).toBeInTheDocument();expect(screen.getByRole("group",{name:"Choose and play shot"})).toBeInTheDocument();expect(screen.getByText("WASD or arrows",{exact:false})).toBeInTheDocument();canvas.mockRestore()})});
